# Quy Trình Tiền Xử Lý Dữ Liệu & Giải Mã Phản Biện Reviewer

> [!abstract] Mục đích của File 6
> File này đúc kết toàn bộ logic của notebook `preprocess.ipynb`: **Quy trình 5 bước tiền xử lý chuẩn mực**, **Ý đồ khoa học đằng sau sự bất đối xứng giữa Train và Test (17 mã độc Zero-Day, 2 mã độc cũ `warezclient`/`spy`)**, **Thuật toán lấy mẫu phân tầng bảo vệ tấn công hiếm (Largest-Remainder)**, và **Vũ khí phản biện đanh thép trước 3 nghi ngờ của Reviewer IEEE TETC**.

---

## Phần 1: Quy Trình 5 Bước Tiền Xử Lý Dữ Liệu Chuẩn Mực

Toàn bộ quy trình trong hàm `preprocess_nsl_kdd()` được thiết kế theo nguyên tắc **Zero Data Leakage (Không rò rỉ dữ liệu)**:

```text
[1. LOAD RAW DATA]
   KDDTrain+.txt (125,973 dòng)  &  KDDTest+.txt (22,544 dòng)
   ──> Loại bỏ cột 'difficulty_level' (không dùng trong phân loại).
         │
         ▼
[2. TẠO 3 CẤP ĐỘ NHÃN MỚI TỪ CỘT 'LABEL' GỐC]
   Từ 1 cột 'label' gốc, ta sinh thêm 3 cột nhãn mới:
   • label_binary    : 0 (Normal), 1 (Attack) ──> Huấn luyện QSVM (C1–C3).
   • attack_category : Normal, DoS, Probe, R2L, U2R ──> Phân tầng lấy mẫu.
   • label_multiclass: neptune, buffer_overflow, ... (39 loại) ──> Đánh giá chi tiết C4.
   👉 Tổng cộng ta có 4 CỘT NHÃN: ['label', 'label_binary', 'attack_category', 'label_multiclass'].
         │
         ▼
[3. ONE-HOT ENCODING (OHE) CHO 3 CỘT DẠNG CHỮ]
   Mã hóa 3 cột: 'protocol_type', 'service', 'flag' ──> Biến 41 đặc trưng gốc thành 122 cột số.
   ──> Kỹ thuật Reindex: X_test.reindex(columns=X_train.columns, fill_value=0.0)
       (Test chỉ được có các cột Train đã thấy, đảm bảo số chiều Train = Test = 122).
         │
         ▼
[4. CHUẨN HÓA MINMAXSCALER [0, 1] — ZERO LEAKAGE]
   • scaler.fit_transform(X_train) ──> CHỈ fit trên Train.
   • scaler.transform(X_test) + np.clip(..., 0, 1) ──> Áp dụng trên Test.
         │
         ▼
[5. GHÉP NỐI & XUẤT FILE]
   Ghép 122 đặc trưng số + 4 cột nhãn ──> Lưu NSL_KDD_Train_Cleaned.csv (125,973 dòng x 126 cột).
```

### 🔍 Vì sao ở bước này dùng `MinMaxScaler[0, 1]` thay vì `StandardScaler`?
1. **Bảo toàn 84 cột cờ nhị phân OHE $\{0, 1\}$:** `MinMaxScaler` giữ nguyên ý nghĩa $0$ (không có) và $1$ (có). `StandardScaler` sẽ làm méo mó thành các số âm/dương kỳ dị ($-0.05$ và $+18.4$).
2. **Xử lý dữ liệu mạng lệch đuôi (Heavy-tailed):** Các gói tin DoS nổ hàng triệu bytes sẽ làm $\sigma$ của `StandardScaler` bị kéo dãn cực đại, ép $95\%$ dữ liệu bình thường co rúm lại gần 0.
3. **Phù hợp với thuật toán lọc SelectKBest:** ANOVA F-test hoạt động chuẩn xác nhất trên thang đo đồng nhất $[0, 1]$.

---

## Phần 2: Sự Bất Đối Xứng Giữa Train và Test — Ý Đồ Khoa Học Của Tác Giả

Trong tập dữ liệu gốc NSL-KDD, các tác giả (Tavallaee et al.) **cố tình không chia ngẫu nhiên 80/20**, mà thiết lập một cấu trúc bất đối xứng mô phỏng thực tế an ninh mạng:

```text
┌──────────────────────────────────────┬────────────────────────────────────────────────────────┐
│ Tập Dữ Liệu                          │ Đặc điểm & Số lượng Mã Độc                             │
├──────────────────────────────────────┼────────────────────────────────────────────────────────┤
│ TẬP TRAIN (125,973 mẫu)              │ 23 loại (22 loại tấn công + 1 Normal).                 │
│ (Đại diện cho "Quá khứ")             │                                                        │
├──────────────────────────────────────┼────────────────────────────────────────────────────────┤
│ TẬP TEST (22,544 mẫu)                │ 38 loại (37 loại tấn công + 1 Normal).                 │
│ (Đại diện cho "Tương lai")           │                                                        │
├──────────────────────────────────────┼────────────────────────────────────────────────────────┤
│ 17 LOẠI MỚI TOANH CHỈ CÓ Ở TEST      │ apache2, mailbomb, mscan, saint, snmpguess,            │
│ (Tấn công Zero-Day)                  │ httptunnel, ps, xterm, sqlattack...                    │
├──────────────────────────────────────┼────────────────────────────────────────────────────────┤
│ 2 LOẠI CŨ CHỈ CÓ Ở TRAIN             │ • warezclient (890 mẫu)                                │
│ (Mã độc cũ đã bị triệt tiêu)         │ • spy (2 mẫu)                                          │
└──────────────────────────────────────┴────────────────────────────────────────────────────────┘
```

### 💡 Giải mã 2 câu hỏi khoa học then chốt:

#### 1. Tại sao Test lại có tới 17 loại tấn công mới toanh (Zero-Day)?
* **Ý đồ:** Trừng phạt các thuật toán AI "học vẹt / học tủ".
* Nếu chia ngẫu nhiên thông thường, AI chỉ cần nhớ mặt từng con virus là đạt điểm cao ảo. Khi đưa 17 loại mới vào Test, các mô hình học vẹt sẽ sập điểm số. Chỉ có mô hình học được **không gian bản chất tổng quát (như QSVM trong Đóng góp C4)** mới nhận diện được!

#### 2. Tại sao không xóa `warezclient` (890 mẫu) và `spy` (2 mẫu) ra khỏi Train?
* **"Hạt giống tri thức" cho họ R2L:** Cả họ R2L trong Train chỉ có 995 mẫu. Nếu xóa 890 mẫu `warezclient`, họ R2L chỉ còn 105 mẫu $\to$ Quá ít để AI học được hành vi đột nhập từ xa!
* Nhờ 890 mẫu này, AI học được "dáng dấp" của đòn R2L, từ đó sang tập Test mới **tự suy luận và bắt được các biến thể mới** như `warezmaster` (944 mẫu) và `httptunnel` (133 mẫu)!
* **Liêm chính dữ liệu:** Train là lịch sử thực tế được ghi lại từ mạng phòng thí nghiệm MIT Lincoln Lab, không ai được phép cắt xén dữ liệu lịch sử.

---

## Phần 3: Thuật Toán Lấy Mẫu Phân Tầng Thông Minh `stratified_sample_for_qsvm`

### 3.1 Vì sao KHÔNG DÙNG ĐƯỢC hàm `stratify=y` có sẵn của Scikit-learn?

Thông thường trong Machine Learning, người ta hay dùng `train_test_split(..., stratify=y)`. Nhưng trong bài toán này, **hàm có sẵn của Scikit-learn HOÀN TOÀN BẤT LỰC**:

* Trong 125,973 gói tin Train gốc, lớp `U2R` chỉ chiếm $52$ mẫu ($\mathbf{0.041\%}$).
* Khi ta cần rút ra tập con $N = 100$ mẫu để nạp vào máy tính lượng tử:
  * Scikit-learn tính: $100 \times 0.041\% = 0.041 \text{ mẫu} \implies$ **Làm tròn xuống thành $0$ mẫu!**
* Khi ta cần rút ra tập con $N = 500$ mẫu:
  * Scikit-learn tính: $500 \times 0.041\% = 0.206 \text{ mẫu} \implies$ **Làm tròn xuống thành $0$ mẫu!**
* 👉 **Hậu quả:** Hàm chuẩn của Scikit-learn sẽ **XÓA SỔ HOÀN TOÀN lớp U2R ra khỏi tập Train lượng tử**, khiến AI không bao giờ được học cách bắt đòn tấn công chiếm quyền Root!

---

### 3.2 Ví Dụ Bằng Con Số Thực Tế: 2 Hàm Này Thực Sự Phối Hợp Làm Gì Trong Code?

Giả sử ta cần rút **chính xác $N = 100$ mẫu** từ $125,973$ mẫu với 5 họ tấn công:

```text
                                  BÀI TOÁN: RÚT ĐÚNG 100 MẪU
                                              │
                    ┌─────────────────────────┴─────────────────────────┐
                    ▼                                                   ▼
       [BƯỚC 1: XỬ LÝ LỚP HIẾM]                            [BƯỚC 2: XỬ LÝ LỚP CHÍNH]
   • min_rare = max(5, 100 // 20) = 5                  • Ngân sách còn lại = 100 - 10 = 90 mẫu
   • Lớp hiếm có 2 họ: U2R (52 mẫu) & R2L (995 mẫu)    • Tỷ lệ 3 lớp chính: Normal 53.9%, DoS 36.8%, Probe 9.3%
   • rare_budget = min_rare x 2 = 10 mẫu               • Chia 90 mẫu:
   • Hàm _allocate_exact_counts chia 10 mẫu này:         - Normal: 90 x 53.91% = 48.519 ──> Lấy 48 (dư 0.519)
     - Cấp trước mỗi họ 1 mẫu bảo hiểm (ensure_min_one)  - DoS   : 90 x 36.76% = 33.084 ──> Lấy 33 (dư 0.084)
     - 8 mẫu còn lại chia theo tỷ lệ gốc (95% vs 5%):    - Probe : 90 x  9.33% =  8.397 ──> Lấy  8 (dư 0.397)
       + R2L: 1 + 8 x 95% = 1 + 7.6 ──> 9 mẫu          • Tổng phần nguyên: 48 + 33 + 8 = 89 mẫu (THIẾU 1 MẪU!)
       + U2R: 1 + 8 x  5% = 1 + 0.4 ──> 1 mẫu          • THUẬT TOÁN LARGEST-REMAINDER:
     ──> TỔNG LỚP HIẾM = 9 + 1 = 10 MẪU (10%)!           Phần dư của Normal (0.519) LỚN NHẤT
                                                         ──> Cộng bù +1 mẫu cho Normal thành 49 mẫu!
                                                       ──> Tổng 3 lớp chính: 49 + 33 + 8 = 90 MẪU CHUẨN XÁC!
                    │                                                   │
                    └─────────────────────────┬─────────────────────────┘
                                              │
                                              ▼
                               [BƯỚC 3: GHÉP LẠI TỔNG THỂ]
                       • 10 mẫu lớp hiếm  +  90 mẫu lớp chính  =  100 MẪU
               ──> ĐẠT CHÍNH XÁC 100 MẪU TUYỆT ĐỐI VÀ CÓ ĐỦ CẢ 5 HỌ TẤN CÔNG!
```

---

### 3.3 Cơ chế `min_rare` ở tập $N=1000$ vs các tập nhỏ $N \in [100, 200, 500]$

* **Ở tập chính $N = 1000$ (Cell 5):** Tác giả truyền tham số cố định `min_rare = 30` $\implies$ Ngân sách lớp hiếm chiếm khoảng $60$ mẫu ($6\%$).
* **Ở các tập con nhỏ $N \in [100, 200, 500]$ (Cell 6):** Tác giả dùng công thức động `min_rare = max(5, n // 20)` để co dãn linh hoạt:
  * Khi $N = 100$: `min_rare = max(5, 5) = 5` $\implies$ Khối hiếm $= 10$ mẫu ($10\%$).
  * Khi $N = 200$: `min_rare = max(5, 10) = 10` $\implies$ Khối hiếm $= 20$ mẫu ($10\%$).
  * Khi $N = 500$: `min_rare = max(5, 25) = 25` $\implies$ Khối hiếm $= 50$ mẫu ($10\%$).

👉 Nhờ công thức này, **lớp hiếm U2R luôn có mặt đại diện ($1 \to 3$ mẫu)**, họ R2L có đủ số lượng để học, mà các lớp đa số Normal và DoS cũng **không bị méo mó tỷ lệ**!

---

## Phần 4: 3 Nghi Ngờ Của Reviewer IEEE TETC & Vũ Khí Phản Biện Đanh Thép

Khi ta báo cáo: *"QSVM vượt trội trên các đòn tấn công hiếm ở tập $N=500$ (tăng $+6.7\%$ F1, chỉ số Cohen's $d = 0.68$)"*, Reviewer 1 & 4 đặt ra 3 nghi ngờ:

```text
┌──────────────────────────────────────┬────────────────────────────────────────────────────────┐
│ 3 NGHI NGỜ CỦA REVIEWER              │ VŨ KHÍ ĐÁP TRẢ ĐANH THÉP TRONG REBUTTAL                │
├──────────────────────────────────────┼────────────────────────────────────────────────────────┤
│ 1. Nghi ngờ "Hái quả ngọt"           │ • 5 MULTI-RUN SEEDS ĐỘC LẬP + KIỂM ĐỊNH MCNEMAR:       │
│    (Cherry-picking):                 │   Chạy 5 Seed độc lập (100, 101, 102, 103, 104) để     │
│    "Có phải các anh chạy 50 lần rồi  │   tính Mean ± Std, loại bỏ hoàn toàn yếu tố may rủi.   │
│    chỉ chọn 1 lần may mắn nhất để    │ • Kiểm định McNemar lập bảng đối đầu 2x2, chứng minh   │
│    đưa vào bài báo không?"           │   p-value < 0.05 (số lần QSVM cứu nguy vượt trội).     │
├──────────────────────────────────────┼────────────────────────────────────────────────────────┤
│ 2. Nghi ngờ "Rò rỉ dữ liệu"          │ • CÁCH LY TUYỆT ĐỐI 2 NGUỒN DỮ LIỆU:                   │
│    (Data Leakage giữa các tập con):  │   Tập Train (100, 200, 500) chỉ lấy từ KDDTrain+       │
│    "Liệu các mẫu Test nhỏ có bị trùng│   (125,973 dòng), tập Test (100, 200, 300) chỉ lấy từ  │
│    lẫn vào tập Train không?"         │   KDDTest+ (22,544 dòng) — hoàn toàn không trùng 1 gói!│
├──────────────────────────────────────┼────────────────────────────────────────────────────────┤
│ 3. Nghi ngờ "Mẫu Train U2R quá ít"   │ • CƠ CHẾ KHÓA PHA LƯỢNG TỬ (QUANTUM FEW-SHOT):         │
│    (Chỉ có 2-3 mẫu U2R ở N=500):     │   1. U2R 'đội lốt' Normal: RBF cổ điển chỉ nhìn khoảng │
│    "Chỉ với 2-3 mẫu U2R thì làm sao  │      cách Euclidean nên bị lừa (xếp U2R thành Normal). │
│    mô hình có thể học và nhận diện   │   2. Cổng CNOT (pi-x2)(pi-x3) phát hiện sự lệch pha    │
│    được 67 mẫu U2R ở tập Test?"      │      lượng tử ngầm của shell Root ──> Phân loại đúng!  │
│                                      │   3. Không gian Hilbert 16D chỉ cần 2-3 Support Vector │
│                                      │      là định hình được siêu phẳng phân tách tối ưu!    │
└──────────────────────────────────────┴────────────────────────────────────────────────────────┘
```

---

### 4.1 Phân Biệt Rạch Ròi: "5 Multi-Run Seeds" vs "5-Fold Cross-Validation"

```text
┌──────────────────────────────────────┬──────────────────────────────────────┐
│ 5-FOLD CROSS VALIDATION (5-Fold CV)  │ 5 MULTI-RUN SEEDS                    │
├──────────────────────────────────────┼──────────────────────────────────────┤
│ • Bản chất: "Băm 1 tập dữ liệu cố    │ • Bản chất: "Rút 5 tập dữ liệu hoàn  │
│   định thành 5 phần rồi xoay vòng".  │   toàn độc lập từ tổng thể 126k dòng"│
│                                      │                                      │
│ • Quy trình:                         │ • Quy trình:                         │
│   Lấy 1 tập 500 mẫu, chia 5 phần.    │   Chạy 5 Seed khác nhau (100–104).   │
│   - Lần 1: Train 4 phần, Test 1 phần.│   - Mỗi run rút 500 gói tin khác     │
│   - Lần 2: Train 4 phần, Test phần 2.│     nhau (dù cùng tỷ lệ phân tầng).  │
│   ... xoay vòng 5 lần.               │   - Mỗi run tính Kernel riêng và     │
│                                      │     đánh giá độc lập trên tập Test.  │
│ • Dùng ở đâu?                        │ • Dùng ở đâu?                        │
│   Dùng trong bước TÌM THAM SỐ (C1:   │   Dùng trong ĐÁNH GIÁ CUỐI CÙNG (C3, │
│   chọn K=20 và chọn 4 Qubit PCA).    │   C4, C5, C6) để báo cáo Mean ± Std! │
└──────────────────────────────────────┴──────────────────────────────────────┘
```

---

### 4.2 Vì sao Kiểm định McNemar lại chứng minh "Thắng do Thực Lực"?

Bảng đối đầu $2 \times 2$ của Kiểm định McNemar:
* **$b$:** Số gói tin mà **QSVM đoán đúng** nhưng **Classical SVM đoán sai**.
* **$c$:** Số gói tin mà **Classical SVM đoán đúng** nhưng **QSVM đoán sai**.
* Chỉ số $\chi^2 = \frac{(|b - c| - 1)^2}{b + c}$ cho ra **$p$-value $< 0.05$**.
👉 Điều này khẳng định số lần QSVM cứu nguy ($b$) vượt trội áp đảo có ý nghĩa thống kê so với Classical SVM ($c$), dập tắt hoàn toàn nghi ngờ về "ăn may ngẫu nhiên"!

---

## Phần 5: Bảng Tra Cứu Nhanh File Tạo Ra Từ `preprocess.ipynb`

```text
┌──────────────────────────────────────┬───────────────┬────────────────────────────────────────────────────────┐
│ Tên File Đầu Ra                      │ Kích Thước    │ Mục Đích Sử Dụng                                       │
├──────────────────────────────────────┼───────────────┼────────────────────────────────────────────────────────┤
│ NSL_KDD_Train_Cleaned.csv            │ 125,973 x 126 │ Toàn bộ dữ liệu huấn luyện sạch.                       │
│ NSL_KDD_Test_Cleaned.csv             │ 22,544 x 126  │ Toàn bộ dữ liệu kiểm thử sạch.                         │
│ NSL_KDD_Train_Sample1000.csv         │ 1,000 x 126   │ Dữ liệu đầu vào cho SelectKBest và QSVM chuẩn.         │
│ NSL_KDD_Train_Sample{100,200,500}.csv│ 100/200/500   │ Dữ liệu vẽ Learning Curve / Sample Complexity (C6).    │
│ NSL_KDD_Test_Sample{100,200,300}.csv │ 100/200/300   │ Dữ liệu kiểm thử đa kích thước.                        │
│ multi_run/train_run{1..5}.csv        │ 1,000 x 126   │ 5 tập Train phân tầng độc lập cho đánh giá thống kê.   │
│ minmax_scaler.joblib                 │ Đối tượng     │ Scaler [0, 1] đã fit trên Train để dùng lại.           │
│ feature_columns.csv                  │ 122 dòng      │ Danh sách 122 tên đặc trưng sau One-Hot Encoding.      │
└──────────────────────────────────────┴───────────────┴────────────────────────────────────────────────────────┘
```
