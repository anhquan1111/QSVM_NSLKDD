# Nền Tảng Lượng Tử & Thuật Toán QSVM

> [!abstract] Mục đích của File 1
> File này cung cấp toàn bộ **nền tảng vật lý lượng tử, toán học của mạch ZZFeatureMap**, 3 ví dụ trực quan từng bước, và phân tích sâu về **2 hàm tính toán ma trận Kernel trong Qiskit** (*FidelityStatevectorKernel vs FidelityQuantumKernel*).

---

## Phần 1: Trực giác về Máy tính Lượng tử & Vướng víu

### 1.1 Bit cổ điển vs Qubit — Hệ quy chiếu biểu diễn

| Đặc tính | Máy tính cổ điển | Máy tính lượng tử (QSVM) |
|---|---|---|
| **Đơn vị biểu diễn** | Bit nhị phân: chỉ là $0$ hoặc $1$ rời rạc | Qubit: vector trạng thái liên tục $|\psi\rangle$ trên mặt cầu Bloch |
| **Mã hóa đặc trưng** | Tốn biến số thực số học | **Angle Encoding:** Scale $x_i \in [0, \pi]$ làm góc quay pha lượng tử |
| **Không gian 4 biến** | 4 đặc trưng cổ điển = $4\text{D}$ | **4 Qubit = $16\text{D}$ không gian Hilbert ($2^4 = 16$)** |

> [!tip] Trực giác về Mã hóa Đặc trưng vào Qubit (Angle Encoding)
> * **Nếu là Chữ (`tcp`, `http`):** Đi qua One-Hot Encoding (122D) → SelectKBest (20D) → PCA nén thành 4 số thực $(x_0, x_1, x_2, x_3)$.
> * **Đưa vào Qubit:** Mỗi số thực được scale về $[0, \pi]$ và đóng vai trò như **góc quay của kim đồng hồ** trên từng Qubit! Số thực khác nhau $\to$ Góc quay khác nhau $\to$ Trạng thái lượng tử khác nhau.

---

### 1.2 Superposition (Chồng chất) — Tọa độ mới trong không gian 16 chiều

$$
\begin{aligned}
\textbf{1 Qubit:} \quad & |\psi\rangle = \alpha|0\rangle + \beta|1\rangle \quad \text{với } \alpha^2 + \beta^2 = 1 \\
& \bullet \ \alpha = 1, \beta = 0 \implies \text{Thuần } |0\rangle \text{ (như bit 0 cổ điển)} \\
& \bullet \ \alpha = 0, \beta = 1 \implies \text{Thuần } |1\rangle \text{ (như bit 1 cổ điển)} \\
& \bullet \ \alpha, \beta \neq 0 \implies \text{CHỒNG CHẬP (đồng thời vừa là 0, vừa là 1)} \\[8pt]
\textbf{4 Qubit:} \quad & |\psi\rangle = c_{0000}|0000\rangle + c_{0001}|0001\rangle + \dots + c_{1111}|1111\rangle \quad (16 \text{ trạng thái đồng thời})
\end{aligned}
$$

> [!note] Chồng chập để làm gì trong Machine Learning?
> Không phải để đo ra một chuỗi `01` ngẫu nhiên, mà **toàn bộ 16 hệ số** $c_{0000} \dots c_{1111}$ **chính là tọa độ mới 16 chiều** của gói tin mạng. Ở không gian 4D ban đầu, gói tin tấn công và bình thường bị đan xen (khó phân tách), nhưng khi nâng lên 16D Hilbert chúng tự động tách rời nhau ra để SVM dễ tìm siêu phẳng phân loại!

---

### 1.3 Vướng víu Lượng tử (Quantum Entanglement) — Nối mạng 4 Qubit

> [!important] 4 Qubit không tự động vướng víu khi đứng cạnh nhau!

Ban đầu mọi Qubit đều độc lập ở $|0\rangle$. Muốn chúng vướng víu, ta phải **chủ động áp dụng cổng CNOT (cổng 2-qubit)**:

```text
Mạch ZZFeatureMap với 4 Qubit (Full Entanglement):
Ta nối CNOT giữa TẤT CẢ 6 CẶP QUBIT có thể:
  1. Cặp (0, 1)        4. Cặp (1, 2)
  2. Cặp (0, 2)        5. Cặp (1, 3)
  3. Cặp (0, 3)        6. Cặp (2, 3)

Tổng cộng: 6 cặp x 2 vòng lặp (reps=2) x 2 = 24 cổng CNOT
```

#### Sau khi vướng víu, 4 feature có "chụm làm 1" không?
**ĐÚNG!** Bốn đặc trưng rời rạc $(x_0, x_1, x_2, x_3)$ hòa làm **MỘT HỆ THỐNG LƯỢNG TỬ DUY NHẤT** 16 chiều, chứa:
* Thông tin từng đặc trưng đơn: $x_0, x_1, x_2, x_3$
* TẤT CẢ 6 tích tương tác cặp: $(\pi - x_i)(\pi - x_j)$

> [!tip] Tại sao điều này giải thích lợi thế QSVM (Đóng góp C2 & C3)?
> * **PCA cổ điển** chỉ triệt tiêu tương quan tuyến tính (Pearson = 0), nhưng bỏ lọt tương quan phi tuyến (Spearman = -0.44 giữa PC2-PC3).
> * **Sự vướng víu lượng tử** qua 6 cổng CNOT đã bắt trọn các tương quan phi tuyến này! Khi làm ablation ngắt vướng víu (QSVM-Z), điểm F1 tụt từ `0.8538` xuống `0.8271`.

---

## Phần 2: NISQ là gì & Bản chất Lợi thế Khoa học của Đề tài

### 2.1 Kỷ nguyên NISQ & Hệ sinh thái IBM Qiskit

* **NISQ (Noisy Intermediate-Scale Quantum):** Khái niệm do GS. John Preskill (Caltech) đặt năm 2018 để chỉ **giai đoạn hiện tại (2019 – ~2035)** của công nghệ lượng tử:
  * **Noisy (Nhiễu):** Tỷ lệ lỗi cổng vật lý còn cao ($\approx 0.1\% - 1\%$), chưa có bộ sửa lỗi hoàn chỉnh (Fault-Tolerant).
  * **Intermediate-Scale (Quy mô trung bình):** Mới có từ vài qubit đến vài trăm qubit, chưa đạt hàng triệu qubit.
* **IBM Quantum & Qiskit:**
  * IBM là tập đoàn dẫn đầu thế giới chế tạo chip lượng tử siêu dẫn (*IBM Manila, Lima, Eagle, Heron...*), đặt trong bồn pha loãng siêu lạnh $-273.15^\circ\text{C}$ ($15\text{ mK}$).
  * **Qiskit** là thư viện Python mã nguồn mở của IBM giúp lập trình, gửi mạch và điều khiển máy tính lượng tử theo mô hình Lai (CPU cổ điển giao tiếp với QPU lượng tử).

---

### 2.2 Cách Đề tài Giả lập Phần cứng IBM trên Laptop

Trong đề tài này ta dùng **2 cấp độ mô phỏng khoa học** chạy trên máy tính cá nhân:

```text
CẤP ĐỘ 1: Mô phỏng Toán học lý tưởng (FidelityStatevectorKernel)
  • Tính toán đại số ma trận 16x16 chuẩn xác 100% bằng CPU qua Qiskit.
  • Không có nhiễu ngẫu nhiên → Dùng để chứng minh tính đúng đắn của thuật toán (C3, C5, C6).

CẤP ĐỘ 2: Mô phỏng Phần cứng NISQ thật có Nhiễu (FakeBackend / Noise Model)
  • IBM đo thông số lỗi vật lý thực tế trên chip thật (ví dụ chip IBM Manila).
  • IBM đóng gói toàn bộ dữ liệu lỗi (lỗi CNOT, thời gian decoherence T1/T2, lỗi đo readout)
    vào cấu hình "bản sao giả lập" gọi là FakeBackend (FakeManila, FakeLima).
  • Qiskit dùng profile lỗi này để mô phỏng chính xác hành vi máy thật trên laptop của ta! (C2, C4).
```

> [!important] Ý nghĩa của danh xưng "NISQ-Aware" trong Tựa bài báo
> Bài báo mang tên: *"NISQ-Aware Quantum Kernel SVM..."*
> Chữ **NISQ-Aware** có nghĩa là: **"Nhận thức rõ ràng các giới hạn thực tế của phần cứng thời kỳ NISQ"**:
> * Cố định đúng **4 qubit** (phù hợp năng lực chip NISQ).
> * Tối ưu hóa số cổng CNOT ít nhất có thể (đóng góp C1 Pareto).
> * Thuật toán đã được kiểm chứng chịu đựng tốt dưới mô hình nhiễu thực tế của chip IBM!

---

### 2.3 Nút Thắt Cổ Chai $O(N^2)$ & Lợi thế Thực sự của QSVM (Q1/Q2)

> [!caution] QSVM KHÔNG cạnh tranh về thời gian huấn luyện!
> Trong bài báo khoa học, ta không bao giờ tuyên bố QSVM train nhanh hơn SVM cổ điển:
> * Để tính bảng Ma trận Kernel $N \times N$, máy phải so sánh $\frac{N(N-1)}{2}$ cặp mẫu dữ liệu $\longrightarrow$ Độ phức tạp $O(N^2)$ khiến thời gian huấn luyện lâu.
> * **Lợi thế khoa học thực sự của đề tài nằm ở:**
>   1. **Lợi thế Biểu diễn (Expressivity - C3):** Bắt trọn tương quan phi tuyến Spearman $|\rho_S| = 0.44$ trong không gian 16D Hilbert.
>   2. **Lợi thế Độ phức tạp mẫu (Sample Complexity - C6):** Chỉ cần ít dữ liệu huấn luyện hơn ($N = 300 - 500$) để đạt F1 tối ưu.
>   3. **Lợi thế Độ tin cậy (Reliability / Calibration - Paper 2):** Dự đoán có độ tự tin chuẩn xác, không bị overconfident ở các đòn tấn công mạng hiếm (U2R/R2L).

---

## Phần 3: ZZFeatureMap, Mạch Nghịch Đảo & Cơ Chế Đo Đạc Lượng Tử

### 3.1 Ví dụ 1: Chi tiết 100% Mạch 3-Qubit qua Đủ 2 Reps (8 Trạng thái)

Xét không gian 3 Qubit ($2^3 = 8$ trạng thái cơ sở: $|000\rangle \dots |111\rangle$) với 3 đặc trưng $(x_0, x_1, x_2)$:

#### 🟢 BẮT ĐẦU:
Hệ thống ở trạng thái cơ bản duy nhất: $|\psi_0\rangle = |000\rangle$.

---

#### 🔵 REPS 1 (TẦNG 1):

* **Bước 1.1: Áp dụng 3 Cổng Hadamard ($H^{\otimes 3}$)**
  * Qubit 0: $|000\rangle \to \frac{1}{\sqrt{2}}|000\rangle + \frac{1}{\sqrt{2}}|100\rangle$
  * Qubit 1: Nở tiếp thành 4 trạng thái: $\frac{1}{2}(|000\rangle + |010\rangle + |100\rangle + |110\rangle)$
  * Qubit 2: Nở trọn vẹn thành 8 trạng thái chia đều:
    $$|\psi_1\rangle = \frac{1}{\sqrt{8}} \sum_{s \in \{0, 1\}^3} |s\rangle = \frac{1}{\sqrt{8}} \big( |000\rangle + |001\rangle + |010\rangle + |011\rangle + |100\rangle + |101\rangle + |110\rangle + |111\rangle \big)$$

* **Bước 1.2: Áp dụng 3 Cổng $Z(x_i)$ để nạp góc quay đặc trưng đơn**
  * Qubit 0 quay $x_0 \implies$ Nhân $e^{i x_0}$ vào các trạng thái có bit 0 là `1` ($|100\rangle, |101\rangle, |110\rangle, |111\rangle$).
  * Qubit 1 quay $x_1 \implies$ Nhân $e^{i x_1}$ vào các trạng thái có bit 1 là `1` ($|010\rangle, |011\rangle, |110\rangle, |111\rangle$).
  * Qubit 2 quay $x_2 \implies$ Nhân $e^{i x_2}$ vào các trạng thái có bit 2 là `1` ($|001\rangle, |011\rangle, |101\rangle, |111\rangle$).

* **Bước 1.3: Áp dụng 3 Cặp CNOT & $ZZ$ để khóa Vướng víu cặp**
  * Cặp (0, 1): Nạp góc $p_{01} = (\pi - x_0)(\pi - x_1)$ vào trạng thái có cả Qubit 0 và 1 là `1` ($|110\rangle, |111\rangle$).
  * Cặp (0, 2): Nạp góc $p_{02} = (\pi - x_0)(\pi - x_2)$ vào trạng thái có cả Qubit 0 và 2 là `1` ($|101\rangle, |111\rangle$).
  * Cặp (1, 2): Nạp góc $p_{12} = (\pi - x_1)(\pi - x_2)$ vào trạng thái có cả Qubit 1 và 2 là `1` ($|011\rangle, |111\rangle$).

$$
\begin{aligned}
\text{Trạng thái sau Rep 1:} \quad |\psi_{\text{Rep1}}\rangle = \frac{1}{\sqrt{8}} \Big( & |000\rangle + e^{i x_2}|001\rangle + e^{i x_1}|010\rangle + e^{i(x_1 + x_2 + p_{12})}|011\rangle \\
& + e^{i x_0}|100\rangle + e^{i(x_0 + x_2 + p_{02})}|101\rangle + e^{i(x_0 + x_1 + p_{01})}|110\rangle \\
& + e^{i(x_0 + x_1 + x_2 + p_{01} + p_{02} + p_{12})}|111\rangle \Big)
\end{aligned}
$$

---

#### 🟣 REPS 2 (TẦNG 2):

* **Bước 2.1: Áp dụng lại 3 Cổng Hadamard $\implies$ GIAO THOA LƯỢNG TỬ**
  * Cổng $H$ tác động lên từng Qubit, ghép cặp các trạng thái chỉ khác nhau đúng 1 bit theo quy tắc $0 \to \frac{0+1}{\sqrt{2}}$ và $1 \to \frac{0-1}{\sqrt{2}}$.
  * Ví dụ trên Qubit 0:
    * Nhánh mới của $|000\rangle$ nhận: $\frac{1}{\sqrt{2}}\big( \text{hệ số}(|000\rangle) + \text{hệ số}(|100\rangle) \big) = \frac{1}{4} \big( 1 + e^{i x_0} \big) = \frac{1}{2} e^{i x_0 / 2} \cos(x_0 / 2)$.
    * Nhánh mới của $|100\rangle$ nhận: $\frac{1}{\sqrt{2}}\big( \text{hệ số}(|000\rangle) - \text{hệ số}(|100\rangle) \big) = \frac{1}{4} \big( 1 - e^{i x_0} \big) = -\frac{i}{2} e^{i x_0 / 2} \sin(x_0 / 2)$.
  * 👉 **Giao thoa:** Các hệ số biến thành hàm sóng lượng giác $(\cos, \sin)$. Những nhánh cùng hướng được cộng hưởng phình to, những nhánh ngược hướng bị triệt tiêu!

* **Bước 2.2: Áp dụng lại 3 Cổng $Z(x_i)$ lần 2**
  * Nạp lại 3 góc quay $(x_0, x_1, x_2)$ vào các nhánh đã qua giao thoa để tăng cường độ phức tạp của pha.

* **Bước 2.3: Áp dụng lại 3 Cặp CNOT & $ZZ$ lần 2 $\implies$ TƯƠNG TÁC BẬC CAO**
  * Chạy tiếp 3 cặp CNOT với góc tương tác $p_{01}, p_{02}, p_{12}$.
  * Nhờ đã qua bước giao thoa ở Bước 2.1, các số hạng tương tác bậc 2 bị nhân chéo và móc nối lại với nhau, sinh ra các số hạng mang thông tin của **CẢ 3 ĐẶC TRƯNG CÙNG LÚC**:
    $$\text{Tương tác bậc 3:} \quad x_0 \cdot x_1 \cdot x_2$$

---

### 3.2 Ví dụ 2: Cơ Chế Tự Hadamard rồi Đảo Ngược về $|000\rangle$ (Không cần Gói B)

> [!question] Nếu ta vừa mới Hadamard xong mà muốn quay ngược lại vạch xuất phát $|000\rangle$ thì làm sao?

Câu trả lời: **Chỉ cần áp dụng 3 Cổng Hadamard một lần nữa!** (Vì cổng $H$ tự đảo ngược chính nó: $H \cdot H = I$).

#### 1. Khai triển 3 ngoặc khi qua 3 cổng H:
Biểu thức biến đổi của một trạng thái $|s_0 s_1 s_2\rangle$ là:
$$\big( |0\rangle + (-1)^{s_0}|1\rangle \big) \otimes \big( |0\rangle + (-1)^{s_1}|1\rangle \big) \otimes \big( |0\rangle + (-1)^{s_2}|1\rangle \big)$$

*Ví dụ với trạng thái $|001\rangle$ ($s_0=0, s_1=0, s_2=1$):*
$$\big(|0\rangle + |1\rangle\big) \big(|0\rangle + |1\rangle\big) \big(|0\rangle - |1\rangle\big) = |000\rangle - |001\rangle + |010\rangle - |011\rangle + |100\rangle - |101\rangle + |110\rangle - |111\rangle$$
*(Chú ý: Bất kỳ trạng thái nào có đuôi tận cùng là `1` đều dính dấu trừ vì $(-1)^1 = -1$)*.

#### 2. Bảng Ma Trận Khai Triển 8 Dòng:

```text
Trạng thái xuất phát     Khai triển sau khi qua 3 cổng H (đều nhân 1 / căn 8)
--------------------------------------------------------------------------------------
H lên |000>             +|000> +|001> +|010> +|011> +|100> +|101> +|110> +|111>
H lên |001>             +|000> -|001> +|010> -|011> +|100> -|101> +|110> -|111>
H lên |010>             +|000> +|001> -|010> -|011> +|100> +|101> -|110> -|111>
H lên |011>             +|000> -|001> -|010> +|011> +|100> -|101> -|110> +|111>
H lên |100>             +|000> +|001> +|010> +|011> -|100> -|101> -|110> -|111>
H lên |101>             +|000> -|001> +|010> -|011> -|100> +|101> -|110> +|111>
H lên |110>             +|000> +|001> -|010> -|011> -|100> -|101> +|110> +|111>
H lên |111>             +|000> -|001> -|010> +|011> -|100> +|101> +|110> -|111>
```

#### 3. Cộng dồn theo từng cột (với hệ số $\frac{1}{\sqrt{8}} \times \frac{1}{\sqrt{8}} = \frac{1}{8}$):

$$
\begin{aligned}
\text{Cột } |000\rangle : & \quad \frac{1}{8} (+1 +1 +1 +1 +1 +1 +1 +1) = \frac{8}{8} = \mathbf{1.0 \ (100\% \ - \text{Cộng hưởng toàn phần!})} \\
\text{Cột } |001\rangle : & \quad \frac{1}{8} (+1 -1 +1 -1 +1 -1 +1 -1) = \frac{0}{8} = \mathbf{0.0 \ (Triệt \ tiêu \ sạch \ sẽ!)} \\
\text{Cột } |010\rangle : & \quad \frac{1}{8} (+1 +1 -1 -1 +1 +1 -1 -1) = \frac{0}{8} = \mathbf{0.0 \ (Triệt \ tiêu \ sạch \ sẽ!)} \\
\text{Cột } |100\rangle : & \quad \frac{1}{8} (+1 +1 +1 +1 -1 -1 -1 -1) = \frac{0}{8} = \mathbf{0.0 \ (Triệt \ tiêu \ sạch \ sẽ!)} \\
& \dots \text{Cả 7 cột còn lại đều có đúng 4 dấu cộng và 4 dấu trừ tự triệt tiêu về 0.0!}
\end{aligned}
$$

👉 Toàn bộ năng lượng lượng tử tự động gom trọn vẹn $100\%$ về lại vạch xuất phát $|000\rangle$!

---

### 3.3 Ví dụ 3: So Sánh Gói A và Gói B bằng Mạch Nghịch Đảo (Trừ Pha & Đo Kernel)

Trong Machine Learning, ta cần so sánh **Gói tin A** $\mathbf{a} = (a_0, a_1, a_2)$ và **Gói tin B** $\mathbf{b} = (b_0, b_1, b_2)$:

```text
[XUẤT PHÁT: |000>]
        │
        ▼ 1. MẠCH XUÔI NẠP GÓI A: U(a)
             • 3 cổng H: Nở bung ra 8 trạng thái chia đều.
             • 3 cổng Z(+a): Nạp góc quay đơn (+a0, +a1, +a2).
             • 3 cặp CNOT + ZZ(+pA): Nạp góc vướng víu +p01(a), +p02(a), +p12(a).
        │
        ▼ 2. MẠCH NGƯỢC NẠP GÓI B: U_inv(b) (Đi ngược từ cuối lên với góc âm!)
             • 3 cặp CNOT + ZZ(-pB): Trừ đi góc vướng víu của gói B.
             • 3 cổng Z(-b):         Trừ đi góc quay đơn của gói B.
             • 3 cổng H:             Gom 8 nhánh về lại vạch xuất phát!
        │
        ▼ 3. ĐO ĐẠC ĐỘ TƯƠNG ĐỒNG FIDELITY
             Đo xác suất rơi về chuỗi "000":
             K(a, b) = | Hệ số của |000> |^2
```

#### Cơ chế trừ góc pha:
Sau khi đi qua mạch ngược của B, mỗi nhánh trạng thái mang góc lệch pha chính xác là:
$$\Delta \theta_s = \theta_s(\mathbf{a}) - \theta_s(\mathbf{b})$$

Và khi 3 cổng $H$ cuối cùng gom 8 nhánh về lại $|000\rangle$, hệ số của trạng thái $|000\rangle$ là **trung bình cộng của 8 vector pha**:
$$c_{000} = \frac{1}{8} \sum_{s=1}^8 \exp\Big(i \big[\theta_s(\mathbf{a}) - \theta_s(\mathbf{b})\big]\Big)$$

#### Phân tích 2 trường hợp:
* **Trường hợp 1: Gói A giống hệt Gói B ($\mathbf{a} = \mathbf{b}$):**
  * Mọi $\Delta \theta_s = 0 \implies e^{i \cdot 0} = 1$.
  * $c_{000} = \frac{1}{8}(1 + 1 + 1 + 1 + 1 + 1 + 1 + 1) = \frac{8}{8} = 1.0$.
  * $\implies K(\mathbf{a}, \mathbf{b}) = |1.0|^2 = \mathbf{1.0 \ (100\%)}$.
* **Trường hợp 2: Gói A khác Gói B ($\mathbf{a} \neq \mathbf{b}$):**
  * Các góc $\Delta \theta_s$ quay theo nhiều hướng khác nhau trên mặt phẳng phức $\implies$ Tự triệt tiêu lẫn nhau.
  * $\implies K(\mathbf{a}, \mathbf{b}) \approx \mathbf{0.0}$ (xác suất tràn sang 7 trạng thái còn lại).

> [!tip] Tính đối xứng của Ma trận Kernel
> $K(\mathbf{a}, \mathbf{b}) = K(\mathbf{b}, \mathbf{a})$: Độ tương đồng giữa A và B luôn bằng độ tương đồng giữa B và A $\implies$ Ma trận Kernel $N \times N$ đối xứng qua đường chéo!

---

### 3.4 Tổng quan Mạch ZZFeatureMap & Công thức Tổng quát

Sau khi đã nắm trọn vẹn 3 ví dụ trực quan ở trên, đây là **công thức toán học tổng quát** của mạch ZZFeatureMap dùng trong bài báo:

$$
U_{\Phi(\mathbf{x})} = \left( \prod_{i < j} \exp\Big(i \phi_{ij}(\mathbf{x}) Z_i Z_j\Big) \prod_{i=1}^n \exp\Big(i \phi_i(\mathbf{x}) Z_i\Big) H^{\otimes n} \right)^{\text{reps}}
$$

Trong đó:
* **Góc quay đơn:** $\phi_i(\mathbf{x}) = x_i \longrightarrow$ Mã hóa từng đặc trưng riêng lẻ vào góc pha của Qubit $i$.
* **Góc quay đôi (vướng víu):** $\phi_{ij}(\mathbf{x}) = (\pi - x_i)(\pi - x_j) \longrightarrow$ Mã hóa mối tương tác phi tuyến giữa cặp Qubit $(i, j)$!

```text
Dữ liệu đầu vào: [x0, x1, x2, x3]  (4 PC sau PCA, đã scale về [0, pi])
        │
        ▼  Bước 1: Hadamard Gate (tạo superposition đồng đều 50/50)
        │
        ▼  Bước 2: Pha đơn Z (xi → nạp góc quay vào từng qubit)
        │
        ▼  Bước 3: Pha đôi CNOT + RZZ (xi x xj → vướng víu 6 cặp qubit)
        │
        ▼  Lặp lại 2 lần (reps = 2)
        │
        ▼  Trạng thái lượng tử: |phi(x)> nằm trong không gian 16 chiều!
```

---

### 3.5 Tại sao cần `reps = 2`? (Tầng 1 vs Tầng 2)

`reps = 2` **KHÔNG PHẢI** là chạy xong rồi đo rồi chạy lại, mà là **MẠCH ĐƯỢC XẾP CHỒNG 2 TẦNG NỐI TIẾP NHAU (Độ sâu Depth = 2)** trước khi đo:

* **Tầng 1 (Rep 1):** Nở từ 1 trạng thái lên 16 trạng thái chia đều, nạp dữ liệu đơn $x_i$, và tạo tương tác cặp bậc 2 ($x_i \cdot x_j$). Mạch 1 tầng này quá nông, máy tính cổ điển có thể mô phỏng dễ dàng.
* **Tầng 2 (Rep 2):** 
  * Cổng H ở tầng 2 tạo ra **GIAO THOA LƯỢNG TỬ**: xáo trộn 16 trạng thái, chỗ nào cùng pha thì cộng hưởng khuếch đại, chỗ nào ngược pha thì tự triệt tiêu lẫn nhau.
  * Cổng Z nạp lại dữ liệu lần 2 vào các nhánh đã khuếch đại.
  * Cổng CNOT tầng 2 tạo ra **TƯƠNG TÁC BẬC CAO** (kết nối 3 đến 4 biến số đan xen: $x_0 x_1 x_2 \dots$). Lúc này, bài toán đạt ngưỡng máy tính cổ điển không thể dùng đa thức thông thường để mô phỏng kịp!

---

### 3.6 So Sánh Chuyên Sâu 2 Hàm Tính Ma Trận Kernel trong Qiskit

Trong codebase của dự án, ta sử dụng 2 hàm tính toán Kernel với **cơ chế hoạt động và thời gian chạy hoàn toàn khác nhau**:

| Tiêu chí | `FidelityStatevectorKernel` (Mô phỏng Đại số CPU) | `FidelityQuantumKernel` (Máy Thật / FakeBackend) |
|---|---|---|
| **Cơ chế tính toán** | Vectorized Inner Product (Tích vô hướng đại số 16D) | Ghép mạch liên hoàn $U(a) \to U^\dagger(b)$ & Bắn $8192$ shots |
| **Thời gian tính ($N=500$)** | **Cực nhanh (~1 đến 2 giây)** | **Rất lâu (Hàng chục phút đến hàng giờ)** |
| **Nhiễu (Noise)** | **$0\%$ Nhiễu** (Lý thuyết toán học tuyệt đối) | **Mô phỏng 100% Nhiễu vật lý** (Lỗi CNOT, T1/T2, Readout) |
| **Mục đích trong Bài báo** | Chứng minh tính đúng đắn thuật toán (**C3, C5, C6**) | Chứng minh độ bền trước nhiễu phần cứng NISQ (**C2, C4**) |

```text
┌──────────────────────────────────────────────────────────────────────────────────────────┐
│                      TẠI SAO 1 HÀM CHẠY SIÊU NHANH, 1 HÀM CHẠY SIÊU CHẬM?                │
├──────────────────────────────────────────────────────────────────────────────────────────┤
│ 1. CƠ CHẾ SIÊU TỐC CỦA FidelityStatevectorKernel:                                        │
│    • Bước 1: Qiskit chạy mạch 1 lần cho từng mẫu để lấy vector 16 số phức.               │
│              Với N = 500 mẫu → Chỉ chạy đúng 500 lần (Tốn O(N) phép tính).               │
│    • Bước 2: Gom 500 vector thành ma trận V (kích thước 500 x 16).                       │
│              Tính toàn bộ ma trận Kernel 500x500 bằng phép nhân ma trận đại số tuyến tính│
│              K = |V * V_dagger|^2 qua thư viện C++ BLAS tối ưu CPU → Xong trong 1 giây!  │
│                                                                                          │
│ 2. CƠ CHẾ NẶNG NỀ CỦA FidelityQuantumKernel / Sampler:                                   │
│    • Để tính ma trận 500x500, nó phải ghép 500*499/2 = 124.750 CẶP MẠCH RIÊNG BIỆT!      │
│    • Với MỖI CẶP MẠCH: Nó phải bắn 8192 shots vật lý (đếm số lần rơi về chuỗi "0000").   │
│    • Tổng cộng: 124.750 cặp x 8192 shots = HƠN 1 TỶ PHÁT BẮN GIẢ LẬP NHIỄU VẬT LÝ!        │
│    → CPU phải mô phỏng xác suất sập hàm sóng và suy giảm năng lượng 1 tỷ lần → Rất lâu! │
└──────────────────────────────────────────────────────────────────────────────────────────┘
```

---

### 3.7 Tại sao Ma trận Kernel lại có kích thước vuông (N x N)?

* **Bảng dữ liệu ban đầu:** Là bảng `Mẫu x Feature` (ví dụ: `500 dòng x 4 cột`).
* **Bảng Ma trận Kernel:** Là **BẢNG SO SÁNH CẶP (Pairwise Similarity Matrix)** giữa N mẫu với chính N mẫu đó:
  * Dòng $i$ = Gói tin thứ $i$
  * Cột $j$ = Gói tin thứ $j$
  * Ô $(i, j)$ = Điểm giống nhau giữa Gói $i$ và Gói $j$ (tính từ máy lượng tử)

```text
               Gói 1    Gói 2    Gói 3  ...  Gói N
Gói 1         [ 1.00     0.12     0.85  ...   0.03  ]  ← So sánh Gói 1 với toàn bộ N gói
Gói 2         [ 0.12     1.00     0.05  ...   0.78  ]  ← So sánh Gói 2 với toàn bộ N gói
Gói 3         [ 0.85     0.05     1.00  ...   0.10  ]
...
Gói N         [ 0.03     0.78     0.10  ...   1.00  ]
```

👉 **Kết nối Scikit-Learn:** Khi ta đưa tập train có $N = 500$ mẫu vào, hàm `evaluate` tự động so sánh từng cặp trong 500 mẫu đó và sinh ra ma trận vuông $500 \times 500$. Thuật toán `SVC(kernel='precomputed')` của Scikit-Learn chỉ việc nhìn vào bảng điểm 500x500 này để tìm đường ranh giới phân loại!

---

### 3.8 Đúc kết Trực giác Cốt lõi: Khai báo vs Thực thi

> [!summary] Hai điểm mấu chốt cần nhớ khi đọc Code:
> 1. **Khai báo vs Thực thi:**
>    * `quantum_kernel = FidelityStatevectorKernel(...)` $\longrightarrow$ Chỉ là **khởi tạo bản thiết kế (blueprint)** cấu hình mạch, máy chưa tính toán gì cả.
>    * `quantum_kernel.evaluate(X, X)` hoặc `model.fit()` $\longrightarrow$ Mới là lúc **máy tính thực sự chạy mô phỏng lượng tử $O(N^2)$** để tính từng cặp gói tin và nhả ra ma trận $N \times N$.
> 2. **Bản chất 100 gói tin và Fidelity:**
>    * Có **100 gói tin** 4 chiều cổ điển $(x_0, x_1, x_2, x_3)$.
>    * Qua 6 cổng CNOT vướng víu của `ZZFeatureMap`, 100 gói tin được nhúng thành **100 vector trong không gian Hilbert 16 chiều**.
>    * Hàm `Fidelity` tính mức độ tương đồng giữa 100 gói tin **ngay trong không gian 16 chiều đó** $\longrightarrow$ sinh ra bảng Ma trận Kernel **100 x 100** cho thuật toán SVM cổ điển phân loại!

---

### 3.9 Các Kiểu Cấu Hình Vướng Víu: Full vs Linear vs Circular vs None

Trong thư viện Qiskit, tham số `entanglement` quyết định **sơ đồ nối dây CNOT (Topology)** giữa các Qubit để nạp các góc $p_{ij} = (\pi - x_i)(\pi - x_j)$:

```text
1. FULL (Toàn phần — Cấu hình đề tài chọn):
   Nối TẤT CẢ 6 cặp: (0-1), (0-2), (0-3), (1-2), (1-3), (2-3)
   • Bắt trọn 100% mọi tương quan phi tuyến giữa bất kỳ 2 đặc trưng nào!
   • Chi phí: 6 cặp x 2 (reps) x 2 = 24 cổng CNOT.

2. LINEAR (Đường thẳng):
   Chỉ nối các hạt liền kề: (0-1), (1-2), (2-3)
   • Bỏ rơi hoàn toàn các góc: p02 = 0, p03 = 0, p13 = 0.
   • Ví dụ: Trạng thái |101> chỉ mang pha e^(i * [x0 + x2]) thay vì có thêm +p02!
   • Hậu quả: Tiết kiệm CNOT (12 cổng), nhưng nếu PC0 và PC2 có tương quan phi tuyến ngầm
     thì mô hình sẽ bị "mù" và bỏ lọt đòn tấn công!

3. CIRCULAR (Vòng tròn khép kín):
   Nối (0-1), (1-2), (2-3) và nối thêm vòng khép kín (3-0).

4. NONE / TẮT VƯỚNG VÍU (Mô hình Ablation QSVM-Z):
   Không nối bất kỳ cổng CNOT nào, không có góc p nào cả!
   • 4 Qubit đứng trơ trọi độc lập → F1 tụt từ 0.8538 xuống 0.8271 (Đóng góp C3)!
```

---

### 3.10 Bản Chất Biến Đổi Phi Tuyến: Tại Sao Lượng Tử Biến Phi Tuyến Thành Tuyến Tính?

> [!question] Tại sao các mô hình cổ điển như Neural Network (NN) hay SVM gặp khó ở dữ liệu mạng ít mẫu?

1. **Giới hạn của Neural Network (NN) & XGBoost:**
   * Mặc dù có các hàm phi tuyến (ReLU, Sigmoid), nhưng để xấp xỉ được các mặt cong phi tuyến phức tạp, NN và XGBoost **cần hàng vạn đến hàng triệu mẫu dữ liệu** để tối ưu hàng nghìn trọng số qua Gradient Descent.
   * Khi dữ liệu huấn luyện ít ($N = 300 - 500$, Sample-efficient regime - C6), NN bị overfit nặng nề và bỏ quên các đòn tấn công hiếm ($U2R = 0.04\%$).

2. **Cơ chế "Nhấc bổng không gian" của Lượng tử (Định lý Cover 1965):**
   * **Ở Không gian 4D Ban đầu:** Các điểm Hacker (Đỏ) và Normal (Xanh) nằm lồng vào nhau theo các đường cong phức tạp (hình Parabol, đường tròn). Một mặt phẳng phẳng lì **không thể nào cắt đôi** được đỏ và xanh (Bài toán phi tuyến).
   * **Cổng CNOT và góc vướng víu $p_{ij}$:** Đóng vai trò như chiếc "đòn bẩy":
     * Mối liên hệ bất thường giữa các cặp đặc trưng bị góc $p$ thổi bùng năng lượng, **nhấc bổng các điểm Hacker (Đỏ) lên cao**, trong khi các điểm Normal (Xanh) vẫn nằm ở đáy sàn của Không gian Hilbert 16 chiều.
   * **Ở Không gian 16D Hilbert Mới:** Dữ liệu đã trở thành **PHÂN TÁCH TUYẾN TÍNH (Linearly Separable)**!
   * **Thuật toán SVM cổ điển:** Chỉ việc dùng một siêu phẳng phẳng lì (Hyperplane) chém ngang một phát là tách rời $100\%$ giữa Normal và Attack với chỉ vài trăm mẫu dữ liệu huấn luyện!

---

### 💡 Đúc kết Toàn bộ Trực giác Cốt lõi của QSVM:
```text
Dữ liệu Mạng Gốc (4D)
  │  (Bị uốn lượn cong queo, dính tương quan phi tuyến Spearman |rho_S| = 0.44)
  ▼
Mạch Lượng tử ZZFeatureMap (4 Qubit, reps=2, full)
  │  (Cổng CNOT và góc p nhấc bổng dữ liệu lên Không gian Hilbert 16 chiều)
  ▼
Không gian 16D Hilbert
  │  (Dữ liệu trở nên PHÂN TÁCH TUYẾN TÍNH phẳng phiu!)
  ▼
Bộ Phân Loại Cổ Điển SVC(kernel='precomputed')
  │  (Chém một nhát kiếm siêu phẳng phẳng lì, tìm ra Support Vectors hoàn hảo!)
```
