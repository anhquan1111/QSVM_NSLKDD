# Giải Mã File Cấu Hình Trung Tâm `config.py`

> [!abstract] Mục đích của File 5
> File này giải thích cặn kẽ từng khối tham số trong `config.py`, giải đáp các thắc mắc về **Đường dẫn dữ liệu, Cơ chế lưu Cache của Ma trận Lượng tử, Vì sao bắt buộc dùng MinMaxScaler$[0, \pi]$ thay vì StandardScaler**, và cách xử lý **Tranh luận tham số $C=1.0$ vs $C=10.0$ với Reviewer IEEE TETC**.

---

## Phần 1: Bản Chất Đường Dẫn & Bí Danh Tương Thích Ngược (Lines 13 — 75)

### 1.1 "Vì sao `DATA_RAW_DIR` lại trỏ về NSL-KDD? Có phải bị thiên vị không?"

```python
# NSL-KDD
NSLKDD_RAW_DIR       = DATA_DIR / "nslkdd" / "raw"
NSLKDD_PROCESSED_DIR = DATA_DIR / "nslkdd" / "processed_data"

# UNSW-NB15
UNSW_RAW_DIR       = DATA_DIR / "unsw" / "raw"
UNSW_PROCESSED_DIR = DATA_DIR / "unsw" / "processed_data"

# -- Alias tương thích ngược: code cũ dùng DATA_RAW_DIR (trỏ NSL-KDD) --
DATA_RAW_DIR       = NSLKDD_RAW_DIR
DATA_PROCESSED_DIR = NSLKDD_PROCESSED_DIR
```

* **Lý do lịch sử:**
  * Ban đầu, dự án được khởi tạo và chạy thử nghiệm trên bộ dữ liệu **NSL-KDD** với biến `DATA_RAW_DIR`.
  * Sau này, để nâng cấp Paper 1 theo yêu cầu của Reviewer và viết Paper 2, ta bổ sung thêm bộ dữ liệu thứ hai là **UNSW-NB15**.
  * Cấu trúc thư mục được chia rạch ròi thành 2 nhánh: `data/nslkdd/` và `data/unsw/`.
  * Dòng `DATA_RAW_DIR = NSLKDD_RAW_DIR` chỉ là một **Bí danh tương thích ngược (Backward-Compatibility Alias)** để các file notebook cũ ban đầu không bị báo lỗi sập code (`NameError`).
* **Cách viết chuẩn cho code mới (Dùng Dictionary đa Dataset):**
  ```python
  TRAIN_DATA_PATHS = {
      "NSL-KDD"  : NSLKDD_PROCESSED_DIR / "NSL_KDD_Train_Cleaned.csv",
      "UNSW-NB15": UNSW_PROCESSED_DIR / "UNSW_Train_Cleaned.parquet",
  }
  ```

---

### 1.2 `QSVM_CACHE_DIR` Là Gì? Nó Lưu Trữ Cái Gì Và Được Dùng Ở Đâu?

```python
QSVM_CACHE_DIR = NSLKDD_MODELS_DIR / "qsvm_cache"
```

* **Vấn đề thực tế:** Việc bắn mạch lượng tử tính toán Ma trận Kernel $K$ ($500 \times 500$) tốn từ **1 đến 4 tiếng đồng hồ CPU**. Nếu mỗi lần chạy notebook lại phải tính lại từ đầu thì sẽ mất hàng ngày trời!
* **Cơ chế hoạt động của Cache:**
  * Lần đầu tiên chạy: Máy tính lượng tử tính ra Ma trận vuông $K_{\text{train}}$ ($500 \times 500$) và lưu thành file nhị phân **`.npy` hoặc `.json`** vào thư mục `models/nslkdd/qsvm_cache/`.
  * Từ lần thứ hai trở đi (ví dụ: khi khảo sát tham số $C$, vẽ biểu đồ, tính ECE): Code sẽ kiểm tra xem file `.npy` đã có trong cache chưa. Nếu có rồi $\implies$ **Load file chỉ mất 0.05 giây và chạy tiếp ngay lập tức!**

```python
# Đoạn code thực tế kiểm tra Cache trong notebook C3/C4:
cache_file = QSVM_CACHE_DIR / f"kernel_matrix_train_N{N_train}_reps{ZZ_REPS}.npy"

if cache_file.exists():
    print(f"--> [CACHE HIT] Đã có sẵn ma trận! Load từ: {cache_file}")
    K_train = np.load(cache_file)
else:
    print(f"--> [CACHE MISS] Chưa có, đang chạy mạch Qiskit để tính...")
    K_train = quantum_kernel.evaluate(X_train, X_train)
    np.save(cache_file, K_train)  # Lưu lại cho lần sau dùng
```

---

## Phần 2: Tiền Xử Lý Dữ Liệu: Vì Sao Bắt Buộc Dùng `MinMaxScaler[0, π]`?

> [!important] Câu hỏi then chốt: Có được dùng `StandardScaler` không?
> **KHÔNG ĐƯỢC DÙNG `StandardScaler`! BẮT BUỘC PHẢI DÙNG `MinMaxScaler(feature_range=(0, np.pi))`!**

```python
# Trong config.py:
N_QUBITS  = 4
ANGLE_MAX = np.pi  # Giới hạn trên của MinMaxScaler → [0, π]
```

### 🔍 Giải thích trực giác vật lý:
1. **Tại sao `StandardScaler` bị cấm?**
   * `StandardScaler` chuẩn hóa dữ liệu theo phân phối chuẩn $\mathcal{N}(0, 1)$ có miền giá trị từ âm vô cùng $(-\infty)$ đến dương vô cùng $(+\infty)$ (ví dụ: $-3.5, +4.8$).
   * Các cổng quay lượng tử $R_z(\theta)$ nhận góc quay $\theta$ là **GÓC TRÊN QUẢ CẦU BLOCH**.
   * Nếu góc quay bị âm hoặc vượt quá $\pi$, hạt Qubit sẽ bị "quay vòng vòng nhiều vòng quanh quả cầu" $\to$ **Làm biến dạng thông tin và gây hiện tượng chồng chéo mất dữ liệu!**

2. **Tại sao `MinMaxScaler[0, π]` là chuẩn mực?**
   * Nó nén toàn bộ 4 đặc trưng của gói tin nằm gọn gàng trong góc từ $0$ độ đến $180$ độ ($0 \to \pi$ radian).
   * Khi đưa góc $x_k \in [0, \pi]$ vào cổng $R_z(2x_k)$, trạng thái Qubit sẽ quét đều từ cực Bắc $|0\rangle$ xuống cực Nam $|1\rangle$ của Quả Cầu Bloch một cách mượt mà và độc nhất!

```python
# Đoạn code chuẩn thực hiện trong preprocess.ipynb:
from sklearn.preprocessing import MinMaxScaler

scaler = MinMaxScaler(feature_range=(0, np.pi))
X_train_scaled = scaler.fit_transform(X_train_pca)  # Chỉ fit trên Train
X_test_scaled  = scaler.transform(X_test_pca)       # Zero-leakage trên Test
```

---

## Phần 3: Tranh Luận Tham Số $C=1.0$ (QSVM) vs $C=10.0$ (Classical SVM)

Trong `config.py`:
```python
C_SVM  = 10.0  # Classical SVM baselines
C_QSVM = 1.0   # Regularization C cho QSVM-ZZ
```

### 🔍 Reviewer 1 & 4 đã chất vấn điều gì?
> *"Tại sao các anh lại ưu ái chỉnh tune SVM cổ điển lên $C=10.0$ trong khi QSVM lại để cứng $C=1.0$? Có phải làm như vậy để tạo kết quả giả hay không?"*

### 💡 Lời giải trình & Cách xử lý trong Revision (Notebook `c3_c_tuning_statevector`):
1. **Bản chất của tham số $C$:**
   * $C$ càng lớn $\to$ Phạt lỗi càng nặng, ép mô hình vẽ đường ranh giới khắt khe.
   * **SVM Cổ Điển (RBF):** Do ranh giới bị mờ, nếu để $C=1.0$ thì mô hình phân loại rất kém $\implies$ Bắt buộc phải đẩy lên $C=10.0$ để ép lề chặt hơn thì mới đạt được $F1 \approx 0.73$.
   * **QSVM Lượng Tử:** Do Không gian Hilbert 16D có độ tương phản pha dốc đứng tự nhiên, nó **hoạt động cực kỳ bền vững ở ngay mức mặc định $C=1.0$ ($F1 \approx 0.83$)**.
2. **Khảo sát độ nhạy $C$-Sensitivity:**
   * Ta đã quét dải $C \in [0.1, 1.0, 10.0, 100.0]$:
     * Dù $C=0.1$ hay $C=100.0$, **QSVM luôn duy trì $F1 \ge 0.82 - 0.85$**.
     * Trong khi đó, SVM cổ điển bị tụt dốc khi $C$ thay đổi.
   👉 Đây là một **Lợi thế về tính ổn định (Robustness) của QSVM**, chứ không phải thiên vị!

---

## Phần 4: Các Biến Số Lượng Tử Cốt Lõi & Code Sử Dụng

Dưới đây là các biến lượng tử quan trọng nhất trong `config.py` và cách chúng được gọi trong code:

```python
┌───────────────────────┬───────────────┬────────────────────────────────────────────────────────┐
│ Biến trong config.py  │ Giá trị chuẩn │ Ý nghĩa Kỹ thuật                                       │
├───────────────────────┼───────────────┼────────────────────────────────────────────────────────┤
│ N_QUBITS              │ 4             │ Cố định 4 qubit = 4 chiều PCA (Ràng buộc phần cứng).   │
├───────────────────────┼───────────────┼────────────────────────────────────────────────────────┤
│ HILBERT_DIM           │ 16            │ 2^4 = 16 trạng thái cơ sở (|0000> đến |1111>).         │
├───────────────────────┼───────────────┼────────────────────────────────────────────────────────┤
│ ZZ_REPS               │ 2             │ 2 tầng mạch (Điểm cân bằng giữa Biểu diễn và Nhiễu).   │
├───────────────────────┼───────────────┼────────────────────────────────────────────────────────┤
│ ZZ_ENTANGLEMENT       │ "full"        │ Nối 6 cặp chéo ──> 24 cổng CNOT tạo tương tác bậc 3,4. │
├───────────────────────┼───────────────┼────────────────────────────────────────────────────────┤
│ RANDOM_STATE          │ 42            │ Đảm bảo mọi kết quả chạy lại đều khớp 100%.            │
└───────────────────────┴───────────────┴────────────────────────────────────────────────────────┘
```

### 💻 Đoạn Code Thực Tế Khởi Tạo Mạch QSVM:

```python
import numpy as np
from qiskit.circuit.library import ZZFeatureMap
from qiskit_machine_learning.kernels import FidelityStatevectorKernel
from sklearn.svm import SVC
from config import N_QUBITS, ZZ_REPS, ZZ_ENTANGLEMENT, C_QSVM

# 1. Khởi tạo Mạch ZZFeatureMap lượng tử 4-Qubit
feature_map = ZZFeatureMap(
    feature_dimension=N_QUBITS,
    reps=ZZ_REPS,
    entanglement=ZZ_ENTANGLEMENT  # "full"
)

# 2. Khởi tạo Hàm đo Kernel Lượng tử Statevector (Dirac V * V^dagger)
quantum_kernel = FidelityStatevectorKernel(feature_map=feature_map)

# 3. Tính toán Ma trận Kernel vuông (500x500)
K_train = quantum_kernel.evaluate(x_vec=X_train_scaled, y_vec=X_train_scaled)

# 4. Huấn luyện SVM cổ điển trên Ma trận tính sẵn
qsvm_model = SVC(kernel="precomputed", C=C_QSVM)
qsvm_model.fit(K_train, y_train)
```

---

## Phần 5: Bảng Tra Cứu Nhanh `config.py`

```text
┌──────────────────────────┬────────────────────────────────────────────────────────────────────────┐
│ Tên Biến trong config    │ Giải thích Ý nghĩa Trực quan                                           │
├──────────────────────────┼────────────────────────────────────────────────────────────────────────┤
│ K_FINAL = 20             │ Chọn 20 đặc trưng tốt nhất từ 122 đặc trưng bằng SelectKBest.          │
├──────────────────────────┼────────────────────────────────────────────────────────────────────────┤
│ VARIANCE_THRESHOLD = 0.85│ Nén PCA phải giữ lại ít nhất 85% lượng thông tin gốc.                  │
├──────────────────────────┼────────────────────────────────────────────────────────────────────────┤
│ SHOTS = 512              │ Số lần bắn phát đo lường trong mô phỏng phần cứng có nhiễu.            │
├──────────────────────────┼────────────────────────────────────────────────────────────────────────┤
│ REPS_Z = 2               │ Mạch ZFeatureMap (bỏ CNOT) để làm đối trọng so sánh Ablation C3.       │
├──────────────────────────┼────────────────────────────────────────────────────────────────────────┤
│ ATTACK_CATEGORY_MAP      │ Bảng ánh xạ 37 mã độc con về 4 họ lớn: DoS, Probe, R2L, U2R.           │
└──────────────────────────┴────────────────────────────────────────────────────────────────────────┘
```
